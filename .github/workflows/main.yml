name: workflow

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'README.md'

jobs:
  integration:
    name: Continuous Integration
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pylint pytest

      - name: Lint Code
        run: |
          echo "Running pylint..."
          pylint --rcfile=.pylintrc $(git ls-files '*.py') || exit $(($? & 35))

      - name: Run Unit Tests
        run: |
          echo "Running pytest..."
          pytest --tb=short --disable-warnings -q || exit $(($? & 35))